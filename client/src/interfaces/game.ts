export interface Game {
    name: string
    coverUrl: string
    screenshotUrl: string
    genres: string
    platforms: string
    first_release_year: number
    involved_companies: string
    summary: string
}